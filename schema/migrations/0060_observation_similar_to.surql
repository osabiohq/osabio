BEGIN TRANSACTION;
  DEFINE TABLE OVERWRITE similar_to TYPE RELATION IN observation OUT observation SCHEMAFULL;
  DEFINE FIELD OVERWRITE similarity ON similar_to TYPE float;
  DEFINE FIELD OVERWRITE created_at ON similar_to TYPE datetime;
COMMIT TRANSACTION;
